# Template Folder

This folder contains a template CSV file that demonstrates the expected format for ShotPattern imports.

Type can be configured on each file transformation within the Garmin2ShotPattern application.
Target will be set during the club configuration process in the setup wizard.
